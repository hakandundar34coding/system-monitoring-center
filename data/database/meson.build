pkgdatadir = join_paths(get_option('prefix'), get_option('datadir'), meson.project_name())

install_data([
    '20-sdio-vendor-model.hwdb',
    'arm.ids',
    'database_license.txt',
    'sdcard.ids',
    ],
    install_dir: join_paths(pkgdatadir, 'database')
)
